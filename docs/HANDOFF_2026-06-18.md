# 2026-06-18 账号切换交接记录

## 当前仓库状态

- 仓库路径：`D:\go web 01\scenic-guide`
- 分支：`codex/plan-implementation-closure`
- 远端 PR：`https://github.com/4evour/Scenic_spot_guide_system/pull/1`
- 当前远端 HEAD：`6c0a297 build: refresh spot admin vue assets`
- 当前本地状态：有未提交草稿，集中在知识库管理页 i18n。

## 已推送到 GitHub 的最新工作

以下提交已经推送到远端分支：

- `39464ec feat: localize route admin page`
- `0504b17 build: refresh route admin vue assets`
- `0d1e744 feat: localize content admin page`
- `fba0013 build: refresh content admin vue assets`
- `57b415a fix: avoid duplicate spot delete confirmation`
- `e23d4e1 build: refresh spot delete vue assets`
- `e83daf5 feat: localize spot admin page`
- `6c0a297 build: refresh spot admin vue assets`

本轮已按用户要求把功能源码提交和构建产物提交拆开，方便后续按模块查 bug。

## 已完成模块

- 路线管理页：完成页面标题、按钮、表格列、难度、时长单位、表单、校验文案 i18n，并新增 `check:admin-routes-i18n`。
- 讲解内容管理页：完成内容类型、表格列、音频状态、抽屉表单、校验文案 i18n，并新增 `check:admin-content-i18n`。
- 景点管理页：完成分类 label、页面说明、表格列、价格、二维码、电子围栏、开关说明、表单和校验文案 i18n，并新增 `check:admin-spots-i18n`。
- 景点管理删除交互：移除二次确认问题，删除按钮现在只走 `useCrudTable.handleDelete` 的统一确认弹窗。

## 已验证过的命令

在路线、讲解内容、景点管理切片提交前，分别跑过：

- `npm.cmd run check`
- `npm.cmd run lint`
- 对应页面的 i18n 检查脚本
- 前端契约检查串联命令
- `git diff --check`
- `npm.cmd run build`

已知 lint 仍有既有 11 个 warning、0 errors，主要在 `MarkdownRenderer.vue`、`GlobalHeader.vue`、`GlobalSider.vue`、`DigitalHumanView.vue`，本轮未改这些无关 warning。

构建通过但仍有既有 Vite warning：

- `live2dcubismcore.min.js` 不是 module script
- `echarts`、`naive-ui` chunk 超 500k

## 当前未提交草稿

本地仍有以下未提交改动，不要直接丢弃：

- `web-vue/src/views/AdminKnowledge.vue`
- `web-vue/src/locales/zh-CN.json`
- `web-vue/src/locales/en-US.json`
- `web-vue/package.json`
- `Makefile`
- `scripts/check-admin-knowledge-insights.mjs`
- `scripts/check-admin-knowledge-i18n.mjs`

草稿目标：补齐知识库管理页剩余 i18n。

草稿已做内容：

- `AdminKnowledge.vue` 已接入 `useI18n`。
- 分类、景点分类、KPI、编辑表单、上传文档、筛选区、知识卡片、AI 分析记录、AI 知识候选等用户可见文案已改为 `adminKnowledge.*`。
- `zh-CN.json` / `en-US.json` 已新增 `adminKnowledge` 文案。
- 新增 `scripts/check-admin-knowledge-i18n.mjs`。
- `scripts/check-admin-knowledge-insights.mjs` 已从检查中文硬编码改为检查 `adminKnowledge.analysis.*` key。
- `package.json` 和 `Makefile` 已接入 `check:admin-knowledge-i18n`。

草稿尚未完成：

- 尚未给这组知识库 i18n 草稿跑验证。
- 尚未提交源码切片。
- 尚未重新构建 `static/vue-app`。
- 尚未提交构建产物。
- 尚未推送知识库 i18n 相关提交。

## 下一账号接手步骤

1. 进入仓库并确认状态：

```powershell
cd "D:\go web 01\scenic-guide"
git status -sb
git log --oneline --decorate -12
```

2. 先检查知识库草稿是否有语法错误：

```powershell
cd "D:\go web 01\scenic-guide\web-vue"
npm.cmd run check:admin-knowledge-i18n
npm.cmd run check:admin-knowledge-insights
npm.cmd run check
npm.cmd run lint
```

3. 如果失败，优先修 `AdminKnowledge.vue`、locale key 或检查脚本，不要动已提交模块。

4. 验证通过后，提交知识库 i18n 源码切片：

```powershell
cd "D:\go web 01\scenic-guide"
git add -- CHANGELOG.md Makefile scripts/check-admin-knowledge-i18n.mjs scripts/check-admin-knowledge-insights.mjs web-vue/package.json web-vue/src/views/AdminKnowledge.vue web-vue/src/locales/zh-CN.json web-vue/src/locales/en-US.json
git commit -m "feat: localize knowledge admin page"
```

5. 再单独构建和提交构建产物：

```powershell
cd "D:\go web 01\scenic-guide\web-vue"
npm.cmd run build
cd "D:\go web 01\scenic-guide"
```

构建后先在 `CHANGELOG.md` 顶部追加“刷新知识库管理构建产物”记录，再执行：

```powershell
git add -- CHANGELOG.md static/vue-app
git commit -m "build: refresh knowledge admin vue assets"
git push
```

6. 最后做一次整体检查：

```powershell
cd "D:\go web 01\scenic-guide"
git status -sb
git diff --check
cd web-vue
npm.cmd run check:data-boundaries; npm.cmd run check:admin-queries; npm.cmd run check:admin-knowledge-insights; npm.cmd run check:admin-knowledge-i18n; npm.cmd run check:admin-content-i18n; npm.cmd run check:admin-settings-i18n; npm.cmd run check:admin-avatar-i18n; npm.cmd run check:admin-query-i18n; npm.cmd run check:admin-qrcode-i18n; npm.cmd run check:admin-reports-i18n; npm.cmd run check:admin-routes-i18n; npm.cmd run check:admin-spots-i18n; npm.cmd run check:admin-users-i18n; npm.cmd run check:dashboard-i18n; npm.cmd run check:digital-human-docs
```

## 重要约束

- 必须中文回复。
- 不做微信小程序，主交付端是 Web/Vue，因为小程序无法完整承载当前数字人链路。
- 每次改项目文件必须更新根目录 `CHANGELOG.md`。
- 继续按模块/功能点提交，源码和构建产物分开提交。
- 不要回滚未理解的现有改动。
- `static/vue-app` hash 文件变动是 Vite 构建正常现象，但仍要单独 commit。
